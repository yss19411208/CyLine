# Map template assets

This folder is reserved for minimap template images used by the automatic
position detector.

The first implementation stores a low-confidence estimate from the uploaded
screenshot crop and allows manual correction. Accurate rotated minimap matching
requires per-map minimap reference images with stable coordinate calibration.

